# 智能问答助手系统 — 功能设计文档

## 1. 系统概述

本系统是一个基于大语言模型的智能问答Web应用，支持用户选择不同的基准知识库进行定向提问。系统由前端交互界面和后台管理功能两部分组成，核心能力是将工程规范文档结构化存储，并在对话过程中结合选定知识库为用户提供精准回答。

---

## 2. 前端界面功能

### 2.1 对话主界面

对话主界面是用户的核心操作区域，采用经典的三栏或上下布局结构，包含以下功能区域：

#### 2.1.1 知识库选择区

- **位置**：界面顶部或左侧边栏上方
- **功能**：提供下拉选择框或卡片式列表，展示当前系统中已创建的所有基准知识库
- **交互逻辑**：
  - 用户点击选择框后展开知识库列表，列表项显示知识库名称和简要描述
  - 选定某个知识库后，该知识库名称高亮显示，并在后续对话中作为上下文依据
  - 切换知识库时，当前对话上下文可以选择保留或清空（根据配置策略）
  - 若系统中暂无知识库，显示空状态提示并引导用户前往后台创建

#### 2.1.2 大语言模型输出框（对话展示区）

- **位置**：界面中央主体区域
- **功能**：展示用户与AI助手的完整对话历史
- **展示规则**：
  - 用户提问以右侧气泡形式展示，显示提问时间和内容
  - AI回答以左侧气泡形式展示，包含回答内容、引用来源标识（如基于某知识库某文档）、生成时间
  - 支持Markdown格式渲染，包括代码块、表格、列表等
  - 超长回答支持展开/收起功能
  - 每条AI回答下方提供复制、重新生成、点赞/点踩等快捷操作按钮
  - 对话历史支持滚动加载，默认保留最近50轮对话上下文

#### 2.1.3 大语言模型输入框（提问区）

- **位置**：界面底部固定区域
- **功能**：用户输入问题并提交给大语言模型
- **交互规则**：
  - 输入框为多行文本域，支持自动增高，最大高度限制为200px
  - 支持粘贴图片和文件，系统将自动解析文件内容并作为问题附件提交
  - 输入框右侧设置发送按钮，支持Enter快捷键发送（Shift+Enter换行）
  - 发送后输入框清空并进入等待状态，显示加载动画
  - 若未选择知识库，发送按钮置灰并提示"请先选择知识库"
  - 输入内容为空或仅包含空白字符时，发送按钮不可用

### 2.2 界面状态说明

| 状态 | 触发条件 | 界面表现 |
|------|----------|----------|
| 初始态 | 用户刚进入页面 | 输出框显示欢迎语和引导提示，输入框可输入，知识库选择框提示"请选择" |
| 对话中 | 用户已发送问题，等待AI回复 | 输入框禁用并显示加载动画，输出框底部显示"正在思考..." |
| 回复完成 | AI已返回完整回答 | 输入框恢复可用，输出框展示完整回答，可继续提问 |
| 知识库为空 | 系统中未配置任何知识库 | 选择框显示"暂无知识库"，输入框禁用，提示前往后台创建 |
| 网络异常 | 请求超时或接口报错 | 输出框显示错误提示，提供重试按钮 |

---

## 3. 后台管理功能

后台管理采用独立的配置页面，仅对管理员角色开放，包含两大核心模块：大语言模型API配置和基准知识库管理。

### 3.1 大语言模型API配置

该模块用于配置系统对接的大语言模型服务参数，决定了前台对话的底层能力来源。

#### 3.1.1 配置字段说明

| 字段名称 | 字段类型 | 是否必填 | 说明 |
|----------|----------|----------|------|
| 配置名称 | 文本 | 是 | 该API配置的标识名称，如"OpenAI-GPT4"、"文心一言"等 |
| 模型提供商 | 枚举 | 是 | 可选值：OpenAI、Azure OpenAI、Anthropic、百度文心、阿里通义、智谱AI、自定义等 |
| API地址 | URL | 是 | 大语言模型接口的请求基地址 |
| API密钥 | 密码文本 | 是 | 访问大语言模型服务的认证密钥，前端展示时脱敏处理 |
| 模型名称 | 文本 | 是 | 具体的模型版本标识，如"gpt-4"、"gpt-3.5-turbo"等 |
| 温度参数 | 数值滑块 | 否 | 控制输出随机性，范围0-2，默认值0.7 |
| 最大令牌数 | 整数 | 否 | 单次请求的最大返回token数，默认2048 |
| 请求超时时间 | 整数 | 否 | 接口请求超时秒数，默认30秒 |
| 是否启用 | 开关 | 是 | 该配置是否生效，同一时间仅允许一个配置处于启用状态 |

#### 3.1.2 业务规则

- 系统必须且只能有一个启用的API配置，若所有配置均禁用，前台对话功能将不可用
- 切换启用的配置时，系统记录切换时间和操作人，便于追溯
- 修改API密钥时需要进行二次确认，避免误操作导致服务中断
- 提供"连接测试"按钮，可实时验证当前配置是否能正常调用模型服务
- 若配置变更导致接口调用失败，系统记录错误日志并发送告警通知

### 3.2 基准知识库管理

该模块用于创建、维护和管理工程规范文档知识库，是前台对话实现精准回答的核心数据支撑。

#### 3.2.1 知识库列表管理

- **列表展示**：以表格形式展示所有知识库，包含知识库名称、描述、文档数量、创建时间、最后更新时间、操作按钮等字段
- **新建知识库**：点击新建按钮，填写知识库名称和描述后创建，名称在同一租户下唯一
- **编辑知识库**：支持修改知识库的名称和描述信息
- **删除知识库**：删除前需二次确认，删除后该知识库下的所有文档及向量数据将被清理，且不可恢复
- **搜索筛选**：支持按知识库名称进行模糊搜索

#### 3.2.2 知识库文档管理

进入某个知识库后，可对其包含的文档进行管理：

- **文档上传**：
  - 支持批量上传文件，单次最多上传20个文件
  - 支持格式：PDF、Word（.doc/.docx）、TXT、Markdown
  - 上传时显示进度条，上传完成后自动触发文档解析和向量化处理
  - 若文件名重复，提示用户选择覆盖或保留两者（自动重命名）

- **文档列表**：
  - 展示文档名称、文件类型、文件大小、上传时间、解析状态、操作按钮
  - 解析状态包括：待解析、解析中、解析成功、解析失败
  - 解析失败的文档提供查看错误原因和重新解析的功能

- **文档操作**：
  - **预览**：在线查看文档解析后的文本内容
  - **下载**：下载原始文件到本地
  - **删除**：删除单个文档，删除后该文档的向量数据同步清理
  - **重新解析**：对解析结果不满意的文档，可手动触发重新解析

#### 3.2.3 文档解析与向量化处理

- 上传的文档经过解析提取纯文本内容，去除格式标签和无意义符号
- 对文本内容进行分块处理，按照固定长度（如500字符）或语义段落进行切分
- 将文本块通过嵌入模型（Embedding Model）转换为向量表示
- 向量数据存储进向量数据库，并建立与原文档的索引关联
- 整个解析过程异步执行，用户无需等待，可在列表中查看处理进度

---

## 4. 核心业务流程

### 4.1 用户提问与回答流程

```
用户选择知识库 → 输入问题 → 前端提交请求 → 后台检索知识库相关片段 → 
拼接系统提示词 + 检索片段 + 用户问题 → 调用大语言模型API → 
流式返回回答 → 前端实时展示 → 对话历史存储
```

**关键规则**：
- 用户提问时，系统首先在选定的知识库向量数据库中进行相似度检索，召回Top-K（默认5条）最相关的文本片段
- 检索到的片段作为上下文背景，与用户问题一并提交给大语言模型
- 模型回答时需在回答末尾标注引用来源，如"参考自《XX规范文档》第3章"
- 若知识库中未检索到相关内容，模型基于自身知识回答，并明确告知用户"未在选定知识库中找到直接依据"

### 4.2 知识库文档入库流程

```
上传文档 → 格式校验 → 异步解析提取文本 → 文本分块 → 
Embedding向量化 → 存入向量数据库 → 更新知识库状态
```

---

## 5. 数据模型设计

### 5.1 核心实体关系

| 实体 | 说明 | 核心字段 |
|------|------|----------|
| LLMConfig | 大语言模型API配置 | id, name, provider, api_url, api_key, model_name, temperature, max_tokens, timeout, is_active, created_at, updated_at |
| KnowledgeBase | 基准知识库 | id, name, description, doc_count, created_at, updated_at |
| Document | 知识库文档 | id, kb_id, file_name, file_type, file_size, file_path, parse_status, parsed_text, chunk_count, created_at, updated_at |
| DocChunk | 文档分块 | id, doc_id, kb_id, content, embedding, chunk_index, created_at |
| Conversation | 对话记录 | id, kb_id, user_id, title, created_at, updated_at |
| Message | 消息记录 | id, conv_id, role(user/assistant), content, references, tokens_used, created_at |

### 5.2 关系说明

- 一个LLMConfig配置在系统中全局生效（单租户场景下）
- 一个KnowledgeBase可包含多个Document
- 一个Document可切分为多个DocChunk
- 一个Conversation关联一个KnowledgeBase，包含多条Message

---

## 6. 接口设计概要

### 6.1 前台对话接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/knowledge-bases | GET | 获取知识库列表 |
| /api/chat | POST | 发送对话消息（流式返回SSE） |
| /api/conversations | GET/POST | 获取/创建对话历史 |
| /api/messages | GET | 获取某对话的消息列表 |

### 6.2 后台管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/admin/llm-configs | GET/POST/PUT/DELETE | LLM配置CRUD |
| /api/admin/llm-configs/test | POST | 测试配置连通性 |
| /api/admin/knowledge-bases | GET/POST/PUT/DELETE | 知识库CRUD |
| /api/admin/knowledge-bases/{id}/documents | GET/POST | 文档列表/上传 |
| /api/admin/documents/{id} | GET/DELETE | 文档详情/删除 |
| /api/admin/documents/{id}/reparse | POST | 重新解析文档 |

---

## 7. 非功能需求

- **响应性能**：知识库检索耗时控制在500ms以内，首字返回时间控制在2秒以内
- **并发支持**：支持至少50个用户同时在线对话
- **文件限制**：单个上传文件大小不超过50MB，总存储空间根据部署环境配置
- **兼容性**：支持Chrome、Firefox、Edge、Safari等主流浏览器最新两个版本
- **数据安全**：API密钥加密存储，用户对话数据隔离，敏感操作记录审计日志

---

## 8. 界面原型示意

### 8.1 前台对话界面布局

```
+----------------------------------------------------------+
|  Logo    智能问答助手                           [用户头像] |
+----------------------------------------------------------+
|                                                          |
|  [请选择知识库 ▼]                                       |
|                                                          |
|  +----------------------------------------------------+  |
|  | 你好！我是智能问答助手，请选择知识库后向我提问。     |  |
|  +----------------------------------------------------+  |
|                                                          |
|  +------------------------------------------+  +-----+   |
|  | 请问钢结构安装的验收标准是什么？           |  |用户A|   |
|  +------------------------------------------+  +-----+   |
|                                                          |
|  +-----+  +------------------------------------------+   |
|  | AI  |  | 根据《钢结构工程施工质量验收规范》，钢结...  |   |
|  |     |  | 构安装的验收标准主要包括以下内容：            |   |
|  |     |  | 1. 主控项目：构件必须符合设计要求...          |   |
|  |     |  | ...                                          |   |
|  |     |  | [参考自《钢结构工程施工质量验收规范》第5章]   |   |
|  +-----+  +------------------------------------------+   |
|                                                          |
+----------------------------------------------------------+
|                                                          |
|  [📎]  [请输入您的问题，Shift+Enter换行...        ] [发送] |
|                                                          |
+----------------------------------------------------------+
```

### 8.2 后台管理 — LLM配置页面布局

```
+----------------------------------------------------------+
|  Logo    智能问答助手            前台 | 后台管理 [退出]     |
+----------------------------------------------------------+
|                                                          |
|  大语言模型API配置                                        |
|  +----------------------------------------------------+  |
|  | 配置名称: [ OpenAI-GPT4                    ]        |  |
|  | 模型提供商: [ OpenAI ▼ ]                             |  |
|  | API地址: [ https://api.openai.com/v1       ]        |  |
|  | API密钥: [ sk-**** **** **** **** ****     ]        |  |
|  | 模型名称: [ gpt-4                          ]        |  |
|  | 温度参数: [====●====] 0.7                            |  |
|  | 最大令牌数: [ 2048 ]                                 |  |
|  | 超时时间: [ 30 ] 秒                                  |  |
|  | 是否启用: [●]                                        |  |
|  |                                                    |  |
|  |  [测试连接]  [保存配置]                              |  |
|  +----------------------------------------------------+  |
|                                                          |
+----------------------------------------------------------+
```

### 8.3 后台管理 — 知识库管理页面布局

```
+----------------------------------------------------------+
|  Logo    智能问答助手            前台 | 后台管理 [退出]     |
+----------------------------------------------------------+
|                                                          |
|  基准知识库管理                              [+ 新建知识库]|
|                                                          |
|  +----------------------------------------------------+  |
|  | 搜索: [                    ] [🔍]                  |  |
|  +----------------------------------------------------+  |
|                                                          |
|  | 知识库名称 | 描述 | 文档数 | 更新时间 | 操作        |  |
|  |------------|------|--------|----------|-------------|  |
|  | 工程规范库 | 包含...|  12   | 2026-07-29| [管理][删除]|  |
|  | 安全标准库 | 包含...|   8   | 2026-07-28| [管理][删除]|  |
|  | ...        | ...  |  ...   | ...      | ...         |  |
|                                                          |
+----------------------------------------------------------+
```

---

## 9. 补充说明

- 本系统为单租户架构设计，若后续需支持多租户，需在数据模型中增加租户ID隔离字段
- 知识库文档的向量化处理依赖Embedding模型，系统默认使用与LLM配置同提供商的嵌入模型，也可独立配置
- 对话历史默认保留90天，超期数据自动归档或清理，具体策略可根据实际存储资源调整
- 系统管理员账号的创建和权限分配不在本文档范围内，建议结合企业现有IAM体系或独立实现RBAC模块