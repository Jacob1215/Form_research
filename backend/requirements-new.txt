# =====================================================================
# 新装 / 更新版本的依赖放这里（不要改 requirements.txt，其构建层走缓存）
#
# 用法：
#   1) 新增依赖：按 requirements.txt 的格式直接写在这里
#   2) 更新版本：把新版行写在这里（pip 会在已装的旧版本基础上升级）；
#      旧版本行可保留在 requirements.txt，也可移出以免混淆
#   3) 不要删除本文件：Dockerfile 的 COPY 依赖它存在（即使为空）
# =====================================================================
# V1.2.1：PPT 制作功能 — Markdown → PPTX
python-pptx==1.0.2
