"""PPT 生成风格（V1.2.7）：自编的 ppt-master 视觉风格描述，随请求注入 claude 提示词。

每个风格是一段中文 prose，描述色调 / 字体 / 形状 / 密度等视觉方向（不写死 HEX，对齐
ppt-master 的 custom style 约定，由 agent 自行解析到 visual_style / mode）。
"""

STYLES: dict[str, dict] = {
    "research": {
        "label": "科研项目汇报",
        "prompt": (
            "科研项目汇报风格：严谨、正式、专业。整体以蓝灰冷色调为主，强调清晰的信息层级与数据可视化；"
            "多用表格、图表、分级编号来组织信息；版式规整、对齐严格；标题用庄重的黑体，正文用易读的无衬线体；"
            "配色克制（深蓝 + 灰 + 少量强调色）；形状以直角卡片、简洁线框为主，装饰性元素少而克制，"
            "突出内容本身与数据结论。"
        ),
    },
    "minimal": {
        "label": "简约",
        "prompt": (
            "简约风格：极简主义、大量留白。以黑白灰为基调，最多一个低饱和强调色；排版干净，"
            "大标题 + 少量正文，信息密度低；用无衬线字体与字重对比（细体标题 / 常规正文）营造层次；"
            "几何元素（细线、圆形、方框）少量点缀；避免多余装饰、阴影与纹理，追求「少即是多」的呼吸感。"
        ),
    },
    "scifi": {
        "label": "AI 科幻",
        "prompt": (
            "AI 科幻风格：未来科技感。深色 / 近黑背景为主，蓝紫霓虹渐变与发光线条作点缀；"
            "大量使用网格、电路、六边形、数据流等科技符号；标题用富有张力的几何无衬线，"
            "标签 / 数据用等宽字体；元素带微弱发光（描边或径向渐变光晕）；层次分明、对比强烈，"
            "营造沉浸式的科技氛围。"
        ),
    },
}

# 供前端展示与后端校验的可用风格 id 列表
STYLE_IDS = ("research", "minimal", "scifi")


def get_style_prompt(style_id: str | None) -> str:
    """返回指定风格的提示词文本；未知 / 空 / 默认 → 空串（不注入风格，自由设计）。"""
    if not style_id:
        return ""
    return STYLES.get(style_id, {}).get("prompt", "")
